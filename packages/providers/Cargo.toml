[package]
name = "sure-providers"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
sure-core = { workspace = true }
async-trait = { workspace = true }
csv = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
utoipa = { workspace = true }
anyhow = { workspace = true }
rust_decimal = { workspace = true }
reqwest = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
akahu-client = "0.2"
# akahu-client depends on reqwest 0.13, one major ahead of the workspace's 0.12 (used by
# the other providers above) — renamed so both can coexist without touching the rest of
# this crate. Feature set mirrors what akahu-client itself requests.
reqwest-akahu = { package = "reqwest", version = "0.13", default-features = false, features = [
    "json",
    "rustls",
] }
zip = { version = "2", default-features = false, features = ["deflate"] }
